export class Benchmarker{

    constructor(){

    }

    // Benchmark each plugin AND one cycle through all plugins (from source to result on UI)
    //  - Latency
    //  - User Performance / Error

}