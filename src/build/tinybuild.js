//create an init script (see example)
//node init.js to run the packager function

export * from './tinybuild/packager.js'
import { packager, defaultServer, initRepo } from './tinybuild/packager.js'

